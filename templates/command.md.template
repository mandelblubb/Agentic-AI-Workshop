---
description: <Was der Befehl tut. Erscheint in der Befehlsübersicht.>
agent: <name-eines-subagenten>
---

<!--
  Vorlage. Kopieren nach .opencode/commands/<befehlsname>.md, ausfüllen,
  diesen Kommentar löschen. Der Dateiname wird zum Befehl: /<dateiname>

  ES GIBT GENAU VIER FRONTMATTER-FELDER
  description, agent, model, subtask. Ein Feld wie "allowed-tools" kennt
  OpenCode nicht — es würde stillschweigend ignoriert, und der Command
  liefe mit vollen Rechten weiter.

  RECHTE HÄNGEN AM AGENTEN, NICHT AM COMMAND
  Soll ein Command nichts ändern dürfen, gebt ihr ihm mit `agent:` einen
  Subagenten, der das nicht darf — einen mit
      permission:
        edit: deny
  in .opencode/agents/<name>.md (siehe subagent.md.template). Ohne `agent:`
  läuft der Command als Hauptagent, mit allem, was der darf. Die Zeile
  `agent:` kann entfallen; dann aber bewusst.

  COMMAND ODER SKILL?
  Command: ihr ruft ihn auf, wenn ihr ihn braucht.
  Skill:   der Agent zieht ihn heran, wenn er passt.

  $ARGUMENTS wird durch alles ersetzt, was hinter dem Befehl steht;
  $1, $2 … greifen einzelne Argumente heraus.
-->

# <Titel>

<Was der Befehl tut, in einem Satz — und was er ausdrücklich nicht tut,
etwa "Ändere dabei keine Dateien.">

## Ablauf

1. <Was zuerst gelesen wird — ggf. mit $ARGUMENTS.>
2. <Nächster Schritt.>
3. <Woran das Ergebnis gemessen wird.>

## Ausgabe

<In welcher Form das Ergebnis kommen soll.>

## Beispiel-Aufruf

```text
/<befehlsname> <argument>
```
